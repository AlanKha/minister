diff --git a/node_modules/react-native-svg/apple/Elements/RNSVGImage.mm b/node_modules/react-native-svg/apple/Elements/RNSVGImage.mm
index 243c24c..d929882 100644
--- a/node_modules/react-native-svg/apple/Elements/RNSVGImage.mm
+++ b/node_modules/react-native-svg/apple/Elements/RNSVGImage.mm
@@ -24,6 +24,7 @@
 
 #import <React/RCTBridge.h>
 #import <React/RCTLog.h>
+#import <React/RCTUIKit.h>
 #import "RNSVGViewBox.h"
 
 #ifdef RCT_NEW_ARCH_ENABLED
@@ -131,7 +132,7 @@ - (void)_setStateAndResubscribeImageResponseObserver:(RNSVGImageShadowNode::Conc
 
 #pragma mark - RCTImageResponseDelegate
 
-- (void)didReceiveImage:(UIImage *)image metadata:(id)metadata fromObserver:(void const *)observer
+- (void)didReceiveImage:(RCTPlatformImage *)image metadata:(id)metadata fromObserver:(void const *)observer
 {
   if (!_eventEmitter || !_state) {
     // Notifications are delivered asynchronously and might arrive after the view is already recycled.
@@ -151,7 +152,7 @@ - (void)didReceiveImage:(UIImage *)image metadata:(id)metadata fromObserver:(voi
              }});
   }
   dispatch_async(dispatch_get_main_queue(), ^{
-    self->_image = CGImageRetain(image.CGImage);
+    self->_image = CGImageRetain(UIImageGetCGImageRef(image));
     self->_imageSize = CGSizeMake(CGImageGetWidth(self->_image), CGImageGetHeight(self->_image));
     [self invalidate];
   });
